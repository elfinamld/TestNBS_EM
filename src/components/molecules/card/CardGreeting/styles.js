import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrap: {
    padding: 15,
    borderRightWidth: 1,
    borderBottomWidth: 1,
    borderRadius: 20,
  },
});

export default styles;
