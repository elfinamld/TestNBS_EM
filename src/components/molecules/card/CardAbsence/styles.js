import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  card: {
    borderWidth: 0.5,
    padding: 15,
    borderRadius: 20,
    marginTop: 10,
    borderRightWidth: 1,
    borderBottomWidth: 1,
  },
});

export default styles;
